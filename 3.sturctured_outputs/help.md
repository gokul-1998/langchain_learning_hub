- if we copy the output from terminal, it has single quotes
- just paste it in a json file in vscode, replace all the single quotes with double quotes and wherever needs change fix it
- right click and do a format json
 